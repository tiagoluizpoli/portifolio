{
  "name": "portifolio",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "guard": "pnpm lint && pnpm typecheck && pnpm test",
    "sync:copilot-skills": "bash ./scripts/sync-copilot-skills.sh",
    "migrator:check": "pnpm --filter appwrite-migrator migrator -- --check",
    "migrator:migrate": "pnpm --filter appwrite-migrator migrator -- --migrate",
    "migrator:seed": "pnpm --filter appwrite-migrator migrator -- --seed",
    "migrator:template": "pnpm --filter appwrite-migrator migrator -- --template"
  },
  "devDependencies": {
    "@biomejs/biome": "2.4.8",
    "@commitlint/cli": "^20.5.0",
    "@commitlint/config-conventional": "^20.5.0",
    "typescript": "^5.7.2"
  }
}