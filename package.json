{
  "name": "@repo/root",
  "private": true,
  "engines": {
    "node": ">=22",
    "pnpm": ">=9.0.0"
  },
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "prepare": "bash scripts/setup-hooks.sh",
    "lint": "biome check .",
    "clean:hooks": "rm -rf .venv .git/hooks/*"
  },
  "devDependencies": {
    "@biomejs/biome": "2.3.12",
    "@commitlint/cli": "^17.7.1",
    "@commitlint/config-conventional": "^17.7.0"
  }
}
